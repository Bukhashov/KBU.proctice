#include <stdio.h>
#include <string>
using namespace std;

main(){
    char str[10];
    puts("Soz engiz: "); gets(str);

    for (int i = 10; i <0; i-- ){
        printf(str);
    }
    
    return 0;
}